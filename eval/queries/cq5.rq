# CQ5: Process type comparison (current implemented query)
#
# This is NOT yet "pathways differentially expressed under Fed-batch vs Perfusion".
# It’s a lightweight proxy that reports how many process instances exist per process type.

PREFIX mcbo: <http://example.org/mcbo#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?processType (COUNT(?process) AS ?count)
WHERE {
  # Get the direct type of each process that is a subclass of CellCultureProcess
  ?process a ?processType .
  ?processType rdfs:subClassOf* mcbo:CellCultureProcess .
  # Note: This query intentionally keeps ?processType to group by specific process types
}
GROUP BY ?processType
ORDER BY DESC(?count) ?processType
