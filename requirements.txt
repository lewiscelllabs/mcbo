# MCBO Python Dependencies
# Conda environment name: mcbo
#
# For creating a new conda environment (mamba recommended for speed):
#   mamba env create -f environment.yml && conda activate mcbo && pip install -r requirements.txt
#   # or with conda:
#   conda env create -f environment.yml && conda activate mcbo && pip install -r requirements.txt
#
# For building documentation locally, also install: pip install -r docs/requirements.txt

rdflib>=7.0.0
pandas>=1.5.0

# Optional: Agent module dependencies (for mcbo-agent-eval)
# Install with: pip install -e python/[agent]
# scipy>=1.10.0
# anthropic>=0.18.0
# openai>=1.0.0
# requests>=2.28.0
